# Runtime (Python 3.9+)
torch==2.8.0
tiktoken==0.14.0
numpy==2.0.2

# Tests
pytest==8.4.2

# GPT-2 weight loading (optional — only needed for src/model/load_weights.py)
transformers==4.57.6
safetensors==0.7.0
huggingface_hub==0.36.2
tokenizers==0.22.2
